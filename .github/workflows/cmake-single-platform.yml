name: Build Program

# Trigger on version tags like v1.0.0
on:
  push:
    tags:
      - 'v*'

env:
  BUILD_TYPE: Release

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # Checkout the repository
    - uses: actions/checkout@v4

    # Install Qt
    - name: Install Qt
      id: install_qt
      uses: jurplel/install-qt-action@v4.3.0
      with:
        cache: 'true'
        version: '6.8.3'

    # Configure CMake
    - name: Configure CMake
      run: cmake -B ${{ github.workspace }}/build -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }} -DCMAKE_INSTALL_PREFIX=$(readlink -f ./install)

    # Build project
    - name: Build
      run: |
         cmake --build build
         cmake --install build

    # Package as AppImage
    - name: Qt to AppImage
      id: appimage
      uses: Kidev/Qt-AppImage@v0.0.8
      with:
        install_folder: './install'
        app_name: 'linux-helper'
        comment: 'Install Helper for LMAOBOX'
        category: 'Utility'
        icon: 'icon.png'
        binary: 'lbox-linux-install-helper'

    # Extract version from the Git tag
    - name: Get Version
      id: get_version
      run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

    # Create and upload GitHub Release
    - name: Create and Upload Release
      uses: softprops/action-gh-release@v2
      with:
        files: ${{ steps.appimage.outputs.appimage }}
        name: LinuxInstallHelper ${{ steps.get_version.outputs.VERSION }}
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
